import React from 'react'

const InputForm = (props) => {
    return (
        <>
            <input {...props} type="text" placeholder="search for a name"/>
        </>
    )
}

export default InputForm
